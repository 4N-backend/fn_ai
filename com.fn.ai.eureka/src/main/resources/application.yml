spring:
  application:
    name: server

server:
  port: 18080

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eurekat:18080/eureka/
  instance:

    hostname: localhost